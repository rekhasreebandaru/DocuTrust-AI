import type { ConfidenceBreakdown } from '../types';
const labels: Array<[keyof ConfidenceBreakdown, string]> = [['overall', 'Overall Confidence'], ['retriever_score', 'Retriever Score'], ['llm_confidence', 'LLM Confidence'], ['context_coverage', 'Context Coverage'], ['hallucination_risk', 'Hallucination Risk']];
export function ConfidencePanel({ confidence }: { confidence: ConfidenceBreakdown }) {
  return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-5">{labels.map(([key, label]) => { const value = Math.round((confidence?.[key] ?? 0) * 100); const danger = key === 'hallucination_risk'; return <div key={key} className="rounded-lg border border-slate-200 bg-white/70 p-3 dark:border-slate-800 dark:bg-slate-950/60"><div className="mb-2 flex items-center justify-between gap-2"><span className="text-xs font-medium text-slate-500">{label}</span><span className={danger ? 'text-sm font-bold text-rose-500' : 'text-sm font-bold text-trust'}>{value}%</span></div><div className="h-2 rounded-full bg-slate-200 dark:bg-slate-800"><div className={danger ? 'h-2 rounded-full bg-rose-500' : 'h-2 rounded-full bg-trust'} style={{ width: Math.min(100, Math.max(0, value)) + '%' }} /></div></div>; })}</div>;
}
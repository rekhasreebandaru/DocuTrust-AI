import { History, LayoutDashboard, ListChecks, LogOut, Moon, Search, Settings, Sun, UploadCloud } from 'lucide-react';
import { NavLink, Outlet } from 'react-router-dom';
import { useAuth } from '../hooks/useAuth';
import { useDarkMode } from '../hooks/useDarkMode';
const baseLinks = [{ to: '/', label: 'Dashboard', icon: LayoutDashboard }, { to: '/upload', label: 'Upload', icon: UploadCloud }, { to: '/chat', label: 'Chat', icon: Search }, { to: '/history', label: 'History', icon: History }, { to: '/settings', label: 'Settings', icon: Settings }];
const adminLinks = [{ to: '/audit', label: 'Audit log', icon: ListChecks }];
export function Layout() { const { logout, username, isAdmin } = useAuth(); const { darkMode, setDarkMode } = useDarkMode(); const links = isAdmin ? [...baseLinks, ...adminLinks] : baseLinks; return <div className="min-h-screen bg-[radial-gradient(circle_at_top_left,#dbeafe,transparent_34%),linear-gradient(135deg,#f8fafc,#eef2f7)] text-slate-950 dark:bg-[radial-gradient(circle_at_top_left,#0f766e33,transparent_32%),linear-gradient(135deg,#020617,#0f172a)] dark:text-slate-100"><aside className="fixed inset-y-0 left-0 z-20 hidden w-72 border-r border-white/50 bg-white/75 p-5 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/80 lg:block"><div className="mb-8"><h1 className="text-3xl font-black text-trust">DocuTrust</h1><p className="text-sm text-slate-500">Enterprise CRAG command center</p></div><nav className="space-y-2">{links.map(({ to, label, icon: Icon }) => <NavLink key={to} to={to} className={({ isActive }) => 'flex items-center gap-3 rounded-lg px-3 py-3 text-sm font-semibold transition ' + (isActive ? 'bg-teal-600 text-white shadow-lg shadow-teal-900/20' : 'text-slate-600 hover:bg-white dark:text-slate-300 dark:hover:bg-slate-900')}><Icon size={18} />{label}</NavLink>)}</nav><div className="absolute bottom-5 left-5 right-5 space-y-3"><button className="btn-secondary w-full" onClick={() => setDarkMode(!darkMode)}>{darkMode ? <Sun size={16} /> : <Moon size={16} />}{darkMode ? 'Light mode' : 'Dark mode'}</button><button className="btn-secondary w-full" onClick={logout}><LogOut size={16} />Logout {username}</button></div></aside><main className="lg:pl-72"><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-10"><div className="mb-4 flex gap-2 overflow-x-auto lg:hidden">{links.map(({ to, label }) => <NavLink key={to} to={to} className="btn-secondary whitespace-nowrap">{label}</NavLink>)}</div><Outlet /></div></main></div>; }
